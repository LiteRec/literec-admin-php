{# Low Stock body partial. Expects: lowStock: list<LowStockAlertView> #}
{% if lowStock|length == 0 %}
    {% include 'components/_empty_state.html.twig' with {
        title: 'All clear',
        message: 'No items are at or below their reorder threshold.',
    } only %}
{% else %}
    <div class="overflow-x-auto">
        <table class="lr-table" data-testid="report-low-stock-table">
            <thead>
                <tr>
                    <th scope="col">Item</th>
                    <th scope="col">Facility</th>
                    <th scope="col" class="text-right">On hand</th>
                    <th scope="col" class="text-right">Threshold</th>
                    <th scope="col" class="text-right">Shortfall</th>
                </tr>
            </thead>
            <tbody>
                {% for alert in lowStock %}
                    <tr data-testid="report-low-stock-row-{{ alert.inventoryItemId }}">
                        <td class="font-mono text-xs text-litrec-text">{{ alert.inventoryItemId|slice(0, 8) }}</td>
                        <td class="text-litrec-text">{{ alert.facilityCode }}</td>
                        <td class="text-right font-mono text-litrec-text">{{ alert.currentOnHandUnits }}</td>
                        <td class="text-right font-mono text-litrec-text-muted">{{ alert.reorderThresholdUnits }}</td>
                        <td class="text-right font-mono text-red-700">{{ alert.shortfallUnits }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    <p class="mt-2 text-xs text-litrec-text-muted" data-testid="report-low-stock-count">
        {{ lowStock|length }} alert{{ lowStock|length == 1 ? '' : 's' }}
    </p>
{% endif %}
