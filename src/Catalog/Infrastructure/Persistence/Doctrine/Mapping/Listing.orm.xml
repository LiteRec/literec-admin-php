<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="App\Catalog\Domain\Listing" table="catalog_listings">

        <unique-constraints>
            <unique-constraint name="UNIQ_catalog_listings_code" columns="code"/>
        </unique-constraints>

        <indexes>
            <index name="IDX_catalog_listings_kind" columns="kind"/>
            <index name="IDX_catalog_listings_archived" columns="archived"/>
        </indexes>

        <!--
            Identity is generated by the aggregate (ListingId, UUID v7) via the
            IdentityGenerator port — no GeneratedValue strategy here.
            Stored as a 36-char string via the catalog_listing_id custom type.
        -->
        <id name="id" type="catalog_listing_id"/>

        <field name="code"          column="code"           type="catalog_listing_code"    nullable="false"/>
        <field name="kind"          column="kind"           type="catalog_listing_kind"    nullable="false"/>
        <field name="name"          column="name"           type="string"                  length="255" nullable="false"/>
        <field name="fees"          column="fees"           type="catalog_fees"            nullable="false"/>
        <field name="taxTreatment"  column="tax_treatment"  type="catalog_tax_treatment"   nullable="false"/>
        <field name="ledgerAccount" column="ledger_account" type="catalog_ledger_account"  nullable="false"/>
        <field name="archived"      column="archived"       type="boolean"                 nullable="false">
            <options>
                <option name="default">false</option>
            </options>
        </field>
        <field name="registeredAt"  column="registered_at"  type="datetime_immutable"      nullable="false"/>
        <field name="updatedAt"     column="updated_at"     type="datetime_immutable"      nullable="false"/>

    </entity>

</doctrine-mapping>
