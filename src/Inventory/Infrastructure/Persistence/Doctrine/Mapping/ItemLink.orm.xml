<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <!--
        ItemLink entity. Identity is generated by the aggregate
        (ItemLinkId, UUID v7) via the IdentityGenerator port — no
        GeneratedValue strategy here.

        Master and linked items are scalar VO columns; no Doctrine
        association across aggregates. The UNIQUE(master_item_id,
        linked_item_id) index enforces the no-duplicate-pair invariant
        at the database level (the InMemory adapter enforces the same
        rule in-process via existsForPair).
    -->
    <entity name="App\Inventory\Domain\ItemLink" table="inventory_item_links">

        <unique-constraints>
            <unique-constraint name="UNIQ_inventory_item_links_pair" columns="master_item_id,linked_item_id"/>
        </unique-constraints>

        <indexes>
            <index name="IDX_inventory_item_links_master" columns="master_item_id"/>
        </indexes>

        <id name="id" type="inventory_item_link_id"/>

        <field name="masterItemId"      column="master_item_id"       type="inventory_item_id"   nullable="false"/>
        <field name="linkedItemId"      column="linked_item_id"       type="inventory_item_id"   nullable="false"/>
        <field name="reservedQuantity"  column="reserved_quantity"    type="inventory_quantity"  nullable="false"/>
        <field name="unlimited"         column="unlimited"            type="boolean"             nullable="false">
            <options>
                <option name="default">false</option>
            </options>
        </field>
        <field name="minRequired"       column="min_required"         type="inventory_quantity"  nullable="false"/>
        <field name="maxPerPurchase"    column="max_per_purchase"     type="inventory_quantity"  nullable="false"/>
        <field name="includeUntil"      column="include_until"        type="datetime_immutable"  nullable="true"/>
        <field name="createdAt"         column="created_at"           type="datetime_immutable"  nullable="false"/>
        <field name="updatedAt"         column="updated_at"           type="datetime_immutable"  nullable="false"/>

    </entity>

</doctrine-mapping>
