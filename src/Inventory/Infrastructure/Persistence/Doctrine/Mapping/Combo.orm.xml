<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <!--
        Combo aggregate root. Identity is generated by the aggregate
        (ComboId, UUID v7) via the IdentityGenerator port — no
        GeneratedValue strategy.

        Cross-context reference to Catalog's Listing is stored as a
        plain listing_id column with a unique constraint — one combo
        per listing.

        ComboComponentEntity child rows are persisted via a
        one-to-many on the components collection with cascade-persist
        + orphan-removal. Combos are small (typically 2–6 components),
        so EAGER fetch keeps round-trips down on the LRA-83 ACL hot
        path that expands combos at sale time.
    -->
    <entity name="App\Inventory\Domain\Combo" table="inventory_combos">

        <unique-constraints>
            <unique-constraint name="UNIQ_inventory_combos_listing" columns="listing_id"/>
        </unique-constraints>

        <id name="id" type="inventory_combo_id"/>

        <field name="listingId"  column="listing_id"  type="catalog_listing_id"     nullable="false"/>
        <field name="archived"   column="archived"    type="boolean"                nullable="false">
            <options>
                <option name="default">false</option>
            </options>
        </field>
        <field name="definedAt"  column="defined_at"  type="datetime_immutable"     nullable="false"/>
        <field name="updatedAt"  column="updated_at"  type="datetime_immutable"     nullable="false"/>

        <one-to-many field="components" target-entity="App\Inventory\Domain\ComboComponentEntity" mapped-by="combo" fetch="EAGER" orphan-removal="true">
            <cascade>
                <cascade-persist/>
            </cascade>
        </one-to-many>

    </entity>

</doctrine-mapping>
