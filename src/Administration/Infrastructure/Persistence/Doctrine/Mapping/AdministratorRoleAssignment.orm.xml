<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <!--
        AdministratorRoleAssignment child entity, owned by Administrator.
        Derived composite identity: (administrator, roleId). `administrator`
        is an association-key id (the many-to-one back to the owning
        Administrator row); `roleId` is a plain UUID column, not a
        foreign key to administration_roles — Role is a separate
        aggregate and the project forbids Doctrine associations (and
        foreign keys, by the same precedent as administration_rank_roles)
        across an aggregate boundary.
    -->
    <entity name="App\Administration\Domain\AdministratorRoleAssignment" table="administration_administrator_roles">

        <indexes>
            <index name="IDX_administration_administrator_roles_role" columns="role_id"/>
        </indexes>

        <id name="administrator" association-key="true"/>
        <id name="roleId" type="administration_role_id" column="role_id"/>

        <field name="assignedAt" type="datetime_immutable" nullable="false" column="assigned_at"/>

        <many-to-one field="administrator" target-entity="App\Administration\Domain\Administrator" inversed-by="roleAssignments" fetch="LAZY">
            <join-column name="administrator_id" referenced-column-name="id" nullable="false" on-delete="CASCADE"/>
        </many-to-one>

    </entity>

</doctrine-mapping>
