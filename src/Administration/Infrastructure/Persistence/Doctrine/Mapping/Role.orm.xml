<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <!--
        Role aggregate root. Identity is generated by the aggregate
        (RoleId, UUID v7) via the IdentityGenerator port — no
        GeneratedValue strategy here.

        No rank identity and no administrator identity is mapped here:
        rank-to-role assignment (LRA-267) and administrator-to-role
        assignment (LRA-269) are owned by the other side of each
        relationship.
    -->
    <entity name="App\Administration\Domain\Role" table="administration_roles">

        <unique-constraints>
            <unique-constraint name="UNIQ_administration_roles_name" columns="name"/>
        </unique-constraints>

        <!--
            The GIN index on privileges (so "which roles grant this
            privilege" is an index lookup rather than a scan) is created
            by raw SQL in the migration, not declared here: Doctrine's
            schema tooling has no first-class GIN index type, the same
            reason the Inventory vendors pg_trgm index
            (Version20260525175800) is raw SQL rather than an
            <indexes> mapping entry.
        -->

        <id name="id" type="administration_role_id"/>

        <field name="name"        column="name"        type="administration_role_name"        nullable="false"/>
        <field name="description" column="description" type="administration_role_description"  nullable="false">
            <options>
                <option name="default"></option>
            </options>
        </field>
        <field name="privileges"  column="privileges"  type="administration_privilege_set"     nullable="false">
            <options>
                <option name="default">[]</option>
            </options>
        </field>
        <field name="retired"     column="retired"     type="boolean"                          nullable="false">
            <options>
                <option name="default">false</option>
            </options>
        </field>
        <field name="createdAt"   column="created_at"  type="datetime_immutable"               nullable="false"/>
        <field name="updatedAt"   column="updated_at"  type="datetime_immutable"               nullable="false"/>
        <field name="version"     column="version"     type="integer"                          version="true" nullable="false">
            <options>
                <option name="default">0</option>
            </options>
        </field>

    </entity>

</doctrine-mapping>
